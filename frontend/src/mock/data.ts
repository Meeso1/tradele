/**
 * Seeded placeholder for data that has no backing endpoint yet.
 *
 * Currently only the "since last submit" portfolio change (see the TODO in
 * `PortfolioService` - the backend doesn't expose the portfolio value at the
 * time of the last submission).
 *
 * Nothing outside `src/services/` may import from here.
 */

export const SINCE_SUBMIT = { abs: 1240, pct: 3.06 };
